services:
  ci-games-api:
    image: ci-games-api
    ports:
      - '5430:5432'
    volumes:
      - ./src/assets/games-init.sql:/docker-entrypoint-initb.d/init.sql
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U postgres -d games']
      interval: 10s
      timeout: 5s
      retries: 5
